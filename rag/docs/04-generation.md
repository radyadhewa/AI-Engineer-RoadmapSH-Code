# 04 - Generation

Goal: run model inference over the augmented prompt.

Key files:

- `lib/ai/gateway.ts`
- `app/api/chat/route.ts`

What to learn:

1. How AI SDK model providers are configured.
2. How Vercel AI Gateway keys/base URL are used.
3. How answer + sources are returned to the client.
